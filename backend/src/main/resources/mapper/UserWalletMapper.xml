<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.campus.backend.mapper.UserWalletMapper">

  <resultMap id="UserWalletResultMap" type="com.campus.backend.model.UserWallet">
    <id column="id" property="id"/>
    <result column="user_id" property="userId"/>
    <result column="balance" property="balance"/>
    <result column="create_time" property="createTime"/>
    <result column="update_time" property="updateTime"/>
  </resultMap>

  <select id="findByUserId" resultMap="UserWalletResultMap">
    SELECT id, user_id, balance, create_time, update_time
    FROM t_user_wallet
    WHERE user_id = #{userId}
    LIMIT 1
  </select>

  <insert id="insert">
    INSERT INTO t_user_wallet (user_id, balance)
    VALUES (#{userId}, 0)
  </insert>

  <update id="deduct">
    UPDATE t_user_wallet
    SET balance = balance - #{amount}, update_time = NOW()
    WHERE user_id = #{userId} AND balance >= #{amount}
  </update>

  <update id="add">
    UPDATE t_user_wallet
    SET balance = balance + #{amount}, update_time = NOW()
    WHERE user_id = #{userId}
  </update>
</mapper>
